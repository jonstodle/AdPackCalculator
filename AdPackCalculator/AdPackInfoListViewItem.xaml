<UserControl x:Class="AdPackCalculator.AdPackInfoListViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdPackCalculator"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300"
			 Padding="5">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

		<TextBlock x:Name="AmountTextBlock" Margin="2"/>

		<TextBlock x:Name="DateTextBlock" FontSize="16" FontWeight="Bold" Grid.Column="1" Margin="2"/>

		<Button x:Name="RemoveButton" Content="Remove" Visibility="Collapsed" Grid.Column="2" Padding="10,1" Margin="2"/>
	</Grid>
</UserControl>
